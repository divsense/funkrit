export makeImportSpec

export type Foom
import type '../libs/ast-types' { ImportSpecifier, Identifier }

newtype Foom = {
    dada: string[]
}

makeImportSpec :: String -> ImportSpecifier
makeImportSpec = name -> ({
    type: 'ImportSpecifier',
    imported: {
        type: 'Identifier',
        name
    },
    local: {
        type: 'Identifier',
        name
    }
})


